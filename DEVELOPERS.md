PROJECT STRUCTURE:

```text
excursium2.0/
│
├── alembic/                      # Alembic directory for managing database migrations
│   ├── versions/                 # Individual migration scripts
│   ├── README                    # Optional documentation for Alembic usage
│   ├── env.py                    # Alembic environment configuration and DB context setup
│   └── script.py.mako            # Template for autogenerated migration scripts
│
├── app/                          # Main application package
│   ├── api/                      # API route definitions grouped by version
│   │   ├── v1/                   # API version 1 routes
│   │   │   ├── carrier.py        # Endpoints related to carrier functionality
│   │   │   ├── client.py         # Endpoints related to client functionality
│   │   │   └── user.py           # Endpoints related to user authentication and profile
│   │
│   ├── core/                     # Core application configuration and setup
│   │   ├── config.py             # Loads environment variables and application settings
│   │   └── oauth.py              # OAuth client registration for social auth (e.g., Google)
│   │
│   ├── db/                       # Database-related setup and utilities
│   │   ├── __init__.py
│   │   ├── base.py               # Base model class used by SQLAlchemy models
│   │   ├── init_db.py            # Optional script to initialize or seed the database
│   │   └── session.py            # Provides the SQLAlchemy async session factory
│   │
│   ├── dependencies/             # Reusable dependencies injected into routes
│   │   ├── context.py            # Common dependency providing user and DB context
│   │   ├── get_db.py             # Dependency for providing a DB session
│   │   └── user.py               # Dependency for retrieving the current user
│   │
│   ├── models/                   # SQLAlchemy models representing DB tables
│   │   ├── __init__.py
│   │   ├── city.py               # City-related models
│   │   ├── docs.py               # Documentation and extra service models
│   │   ├── enums.py              # Enum declarations for model fields
│   │   ├── newsletter.py         # Models for newsletter subscriptions
│   │   ├── order.py              # Order-related models (history, comments, etc.)
│   │   ├── token_state.py        # Models for token and OAuth state storage
│   │   ├── transport.py          # Transport, routes, and schedule models
│   │   └── user.py               # User, Client, and Carrier models
│   │
│   ├── schemas/                  # Pydantic schemas for request/response validation
│   │   ├── __init__.py
│   │   ├── carrier.py            # Schemas for carrier-related data
│   │   ├── client.py             # Schemas for client-related data
│   │   └── user.py               # Schemas for user and auth-related data
│   │
│   ├── services/                 # Business logic layer (service functions)
│   │   ├── __init__.py
│   │   ├── carrier.py            # Logic for carrier actions and updates
│   │   ├── client.py             # Logic for client registration, updates, etc.
│   │   └── user.py               # Logic for user registration, login, email confirmation, etc.
│   │
│   ├── tasks/                    # Background and scheduled tasks
│   │   └── cleanup.py            # Scheduled cleanup tasks (e.g., old records)
│   │
│   ├── utils/                    # Utility and helper modules
│   │   ├── __init__.py
│   │   ├── email.py              # Email sending logic (SMTP integration)
│   │   ├── minio_client.py       # Integration with MinIO for photo and docs storage
│   │   ├── security.py           # Password hashing and security helpers
│   │   ├── tokens.py             # JWT token creation and decoding
│   │   └── validation.py         # Custom field or object validation functions
│   │
│   └── main.py                   # FastAPI application entry point
│
├── .env                          # Environment variables file (used by Pydantic Settings)
├── .gitignore                    # Specifies files/folders ignored by git
├── alembic.ini                   # Alembic configuration file
├── DEVELOPERS.md                 # Developer instructions and project conventions
├── example.env                   # Example environment configuration file
├── README.md                     # Project overview and setup guide
└── requirements.txt              # Python dependency list